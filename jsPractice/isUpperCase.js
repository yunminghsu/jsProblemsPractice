var apple = "Hello";
var testAnswer = false;
function isUpperCase() {
  if (apple.charAt(0) == apple.charAt(0).toUpperCase()) {
    testAnswer = true;
    console.log(testAnswer);
  } else {
    console.log(testAnswer);
  }
}
isUpperCase();
